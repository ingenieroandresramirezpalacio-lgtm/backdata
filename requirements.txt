# DATACONTROL - dependencias del backend (produccion)
#
# Las versiones estan fijadas a proposito: asi la app se comporta igual
# hoy y dentro de un ano, aunque salgan versiones nuevas.

# --- Servidor web / API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Base de datos ---
# El esquema lo maneja Flyway (backend/migraciones). SQLAlchemy solo lee y
# escribe datos: aqui no hay autogeneracion de migraciones.
#
# psycopg[binary] trae una wheel autocontenida (no necesita libpq del sistema
# ni compilar nada): hay versiones para glibc (manylinux, en el CI) y para
# musl (musllinux, en la imagen Alpine). Asi funciona igual en ambos.
sqlalchemy==2.0.36
psycopg[binary]==3.2.3

# --- Seguridad ---
bcrypt==4.2.1
pyjwt==2.10.1

# --- Exportacion a Excel ---
openpyxl==3.1.5

# --- Limite de peticiones (rate limiting) ---
slowapi==0.1.9
